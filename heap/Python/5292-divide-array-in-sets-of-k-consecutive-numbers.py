from typing import  List
import heapq

class Solution:
    def isPossibleDivide(self, nums: List[int], k: int) -> bool:
        # size = len(nums)
        #
        # if size % k != 0:
        #     return False
        #
        # heapq.heapify(nums)
        #
        # while nums:
        #     top = heapq.heappop(nums)
        #
        #     for i in range(1, k):




