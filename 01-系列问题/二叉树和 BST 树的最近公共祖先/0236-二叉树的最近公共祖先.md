注意 `p`，`q` 必然存在树内，且所有结点的值唯一。
递归思想，对以 `root` 为根的子树进行查找 p 和 q , 如果 `root == null`  ，或者  `root is p`  或者 `root is q` 直接返回 `root`。

表示对于当前树的查找已经完毕，否则对左右子树进行查找，根据左右子树的返回值判断：

1、左右子树的返回值都不为 `null`，由于值唯一左右子树的返回值就是 `p` 和 `q`， 此时 `root`为 LCA ；
2、如果左右子树返回值只有一个不为 `null`，说明只有 p 和 q 存在于左或右子树中，最先找到的那个结点为 LCA
3、左右子树返回值均为 `null`， `p` 和 `q` 均不在树中， 返回 `null`。